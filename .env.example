# Server & Hosting Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (Optional: defaults to local JSON file store if empty)
# DATABASE_URL="postgresql://postgres:password@host:5432/warrior_quest_db"

# GEMINI_API_KEY: Required for optional Gemini AI features
GEMINI_API_KEY=""

